import * as Focus from '../src/parts/Focus/Focus.js'

test('focusActivityBar', () => {
  Focus.focusActivityBar()
})

test('focusStatusBar', () => {
  Focus.focusStatusBar()
})

test('focusProblems', () => {
  Focus.focusProblems()
})

test('focusExplorer', () => {
  Focus.focusExplorer()
})

test('focusPreviousTerminal', () => {
  Focus.focusPreviousTerminal()
})

test('focusNextTerminal', () => {
  Focus.focusNextTerminal()
})

test('focusDebugConsole', () => {
  Focus.focusDebugConsole()
})
