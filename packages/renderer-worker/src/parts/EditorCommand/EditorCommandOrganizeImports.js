export const organizeImports = (editor) => {
  console.log('TODO: organize imports')
  // 1. ask extension host for import edits
  // 2. if edits are empty, ignore
  // 3. else, apply import edits and return new editor
  return editor
}
