import * as FileHandle from './FileHandle.js'

export const Commands = {
  'FileHandle.addHandle': FileHandle.addHandle,
  'FileHandle.getHandle': FileHandle.getHandle,
  'FileHandle.removeHandle': FileHandle.removeHandle,
}
