import * as ColorTheme from './ColorTheme.js'

export const Commands = {
  'ColorTheme.hydrate': ColorTheme.hydrate,
  'ColorTheme.setColorTheme': ColorTheme.setColorTheme,
  'ColorTheme.reload': ColorTheme.reload,
}
