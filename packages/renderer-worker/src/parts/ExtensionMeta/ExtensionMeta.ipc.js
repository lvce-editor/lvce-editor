import * as ExtensionMeta from './ExtensionMeta.js'

export const name = 'ExtensionMeta'

// prettier-ignore
export const Commands = {
  addNodeExtension: ExtensionMeta.addNodeExtension,
  addWebExtension: ExtensionMeta.addWebExtension,
}
