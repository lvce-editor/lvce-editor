import * as ExtensionHostSelection from './ExtensionHostSelection.js'

export const name = 'ExtensionHostSelection'

export const Commands = {
  executeGrowSelection: ExtensionHostSelection.executeGrowSelection,
}
