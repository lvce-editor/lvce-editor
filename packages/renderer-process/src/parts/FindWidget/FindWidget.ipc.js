import * as FindWidget from './FindWidget.js'

export const Commands = {
  'FindWidget.create': FindWidget.create,
  'FindWidget.dispose': FindWidget.dispose,
  'FindWidget.setResults': FindWidget.setResults,
}
