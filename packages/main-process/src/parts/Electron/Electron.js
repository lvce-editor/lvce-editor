const {
  BrowserWindow,
  app,
  ipcMain,
  screen,
  dialog,
  protocol,
  session,
  shell,
  clipboard,
  Menu,
} = require('electron')

module.exports = {
  BrowserWindow,
  app,
  screen,
  dialog,
  protocol,
  session,
  shell,
  clipboard,
  ipcMain,
  Menu,
}
