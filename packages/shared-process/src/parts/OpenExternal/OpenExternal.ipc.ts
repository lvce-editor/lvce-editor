import * as OpenExternal from './OpenExternal.ts'

export const name = 'OpenExternal'

export const Commands = {
  openExternal: OpenExternal.openExternal,
  showItemInFolder: OpenExternal.showItemInFolder,
}
