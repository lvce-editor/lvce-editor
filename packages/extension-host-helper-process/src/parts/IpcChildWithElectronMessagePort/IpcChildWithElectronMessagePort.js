import { once } from 'events'

export const listen = async () => {
  // const [message, handle] = await once(process, 'message')
  // if (process.send) {
  //   process.send('ready')
  // }
  // return {
  // }
}

export const wrap = () => {
  // TODO
  return {}
}
