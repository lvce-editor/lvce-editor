# Inline Code Readme

Relative imports, package `main`, conditional `exports`, and CommonJS `require` are supported.

See the [extension guide](https://example.com/extension-guide) for details.

```js
const answer = 42
```
